{
  "name": "counter-contract",
  "version": "1.0.0",
  "description": "A simple counter contract with permission controls for incrementing and decrementing the counter value.",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "deploy:testnet": "ts-node scripts/deploy-testnet.ts",
    "deploy:mainnet": "ts-node scripts/deploy-mainnet.ts",
    "initialize": "ts-node scripts/initialize-contracts.ts"
  },
  "dependencies": {
    "@stacks/transactions": "^2.0.0",
    "@stacks/network": "^2.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^4.0.0"
  },
  "devDependencies": {
    "jest": "^27.0.0",
    "@types/jest": "^27.0.0",
    "ts-jest": "^27.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "author": "",
  "license": "MIT"
}